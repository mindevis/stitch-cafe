# 📚 Документация бота "Вышивальное Кафе"

## 🏗️ Архитектура бота

Бот построен на **aiogram 3.x** и использует модульную структуру с разделением на команды, данные, клавиатуры и базу данных.

---

## 📁 Структура проекта

```
vishivalnoe_kafe/
├── main.py              # Точка входа, инициализация бота
├── config.py            # Конфигурация (токены, ID)
├── database.py          # Работа с SQLite базой данных
├── commands/            # Обработчики команд
│   ├── start.py         # Команда /start
│   ├── order.py         # Логика заказов
│   ├── reset.py         # Сброс данных (админ)
│   └── top.py           # Статистика (админ)
├── data/                # Данные игры
│   ├── dishes.py        # Меню блюд по уровням
│   ├── levels.py        # Уровни и названия
│   ├── special_orders.py # Специальные заказы-события
│   └── texts.py         # Текстовые шаблоны
└── keyboards/           # Клавиатуры
    └── main_menu.py     # Главное меню
```

---

## 🔧 Основные функции бота

### 📌 **main.py** - Точка входа

**Функции:**
- `main()` - Инициализирует бота, регистрирует роутеры, запускает polling

**Зависимости:**
- `commands.start` - роутер старта
- `commands.order` - роутер заказов
- `commands.reset` - роутер сброса
- `commands.top` - роутер статистики

---

### 📊 **database.py** - Работа с базой данных

#### **get_db()** - Контекстный менеджер подключения
```python
async with get_db() as db:
    # Работа с БД
```
- Создаёт/подключается к SQLite базе `data/cafe.db`
- Автоматически применяет миграции
- Закрывает соединение после использования

#### **migrate(db)** - Миграции базы данных
- Создаёт таблицу `users` если её нет
- Поля: `user_id`, `first_name`, `level`, `total_orders`, `total_crosses`, `has_student_done`, `has_dirty_plate_done`, `has_critic_done`, `has_second_chef_done`, `next_order_half`, `last_order_json`, `active_order_json`

#### **ensure_user(db, from_user)** - Создание пользователя
- Добавляет нового пользователя в БД если его нет
- Использует `INSERT OR IGNORE`

#### **fetch_user(db, user_id, first_name)** - Получение пользователя
- Получает данные пользователя из БД
- Автоматически создаёт пользователя если его нет
- Возвращает словарь с данными пользователя

#### **save_active_order(db, user_id, dishes, tag)** - Сохранение активного заказа
- Сохраняет заказ в JSON формате в поле `active_order_json`
- `dishes`: список кортежей `(название, крестики)`
- `tag`: "critic", "student", "dirty_plate" или `None`

#### **get_active_order(db, user_id)** - Получение активного заказа
- Возвращает словарь с `dishes` и `tag` или `None` если заказа нет

#### **clear_active_order(db, user_id)** - Очистка активного заказа
- Удаляет активный заказ пользователя

#### **save_last_order(db, user_id, dishes, order_crosses)** - Сохранение последнего заказа
- Сохраняет последний завершенный заказ для использования в грязной тарелке
- Используется для удвоения заказа при событии "грязная тарелка"

#### **get_last_order(db, user_id)** - Получение последнего заказа
- Возвращает последний завершенный заказ или `None`

#### **finish_order_and_level(db, user_id, tag, order_crosses)** - Завершение заказа и повышение уровня
- Увеличивает `total_orders` на 1
- Обновляет флаги специальных заказов (VIP, студент, торт)
- Повышает уровень если `total_orders % ORDERS_PER_LEVEL == 0` и уровень < MAX_LEVEL
- Возвращает: `(total_orders, level_changed: bool, level_title: str)`

---

### 🍽️ **commands/order.py** - Логика заказов

#### **_iso_year_week()** - Получение ISO года и недели
- Возвращает кортеж `(год, номер_недели)`
- Используется для отслеживания недельной статистики

#### **generate_regular_order(level)** - Генерация обычного заказа
**Логика:**
1. Собирает все доступные блюда из уровней 0..level
2. Выбирает 1 случайное блюдо с текущего уровня
3. Добавляет 2 случайных блюда из открытых уровней (без повторов)
4. Если не хватает блюд, дополняет блюдами с уровня 0
5. Возвращает список из 3 кортежей `(название, крестики)`

#### **_order_index(total_orders)** - Расчёт номера нового заказа
- Возвращает `total_orders + 1` (номер следующего заказа)

#### **new_order(message)** - Обработчик "🧾 Новый заказ"
**Логика работы:**
1. ✅ Проверяет что команда вызвана в правильном чате (`CHAT_ID`)
2. ✅ Проверяет наличие активного заказа (если есть - отказывает)
3. 🎲 Определяет номер заказа и проверяет вероятность специальных заказов:
   - **Студент** (🥡 Лапша): заказы 3-40, 1 раз, 12% шанс
   - **Критик** (🦪 Устрицы): заказы 20-40, 1 раз, 10% шанс
   - **Грязная тарелка**: заказы 3-40, 1 раз, 15% шанс (удваивает предыдущий заказ)
   - **Второй повар**: заказы 3-40, 1 раз, 12% шанс (следующий заказ половинный, не идет в счетчик)
4. 🍽️ Если спецзаказ не выпал - генерирует обычный заказ из 3 блюд
5. 💾 Сохраняет заказ в БД
6. 📤 Отправляет пользователю заказ с информацией

#### **my_order(message)** - Обработчик "📋 Мой заказ"
**Логика:**
1. Проверяет что команда в правильном чате
2. Получает активный заказ пользователя
3. Если заказа нет - сообщает об этом
4. Форматирует и отправляет информацию о текущем заказе

#### **done(message)** - Обработчик "✅ Готово"
**Логика:**
1. ✅ Проверяет чат и наличие активного заказа
2. 📅 Обновляет недельные счётчики
3. ✅ Завершает заказ (обновляет статистику, повышает уровень)
4. 🧹 Очищает активный заказ
5. 🎉 Отправляет сообщение о завершении (с учётом повышения уровня или без)

---

### 🚀 **commands/start.py** - Команда /start

#### **cmd_start(message)** - Обработчик команды /start
- Регистрирует пользователя в БД
- Отправляет приветственное сообщение
- Показывает главное меню

---

### 🔄 **commands/reset.py** - Сброс данных (только админ)

#### **cmd_reset(message)** - Обработчик /reset
**Требования:**
- Только админ (`ADMIN_ID`)
- Только в игровом чате (`CHAT_ID`)

**Действия:**
- Удаляет все записи из таблицы `users`
- Полный сброс игры

---

### 📊 **commands/top.py** - Статистика (только админ)

#### **cmd_top(message)** - Обработчик /top
**Требования:**
- Только админ (`ADMIN_ID`)

**Логика:**
1. Получает всех пользователей из БД
2. Сортирует по `total_orders DESC, level DESC`
3. Форматирует статистику (имя, заказы, уровень, флаги спецзаказов)
4. Отправляет админу в личные сообщения
5. В группе подтверждает отправку

---

### 🎮 **keyboards/main_menu.py** - Главное меню

#### **main_menu_kb()** - Создание клавиатуры
Возвращает ReplyKeyboardMarkup с кнопками:
- `🧾 Новый заказ` (отдельная строка)
- `📋 Мой заказ` | `✅ Готово` (в одной строке)

**Константы:**
- `BTN_NEW` = "🧾 Новый заказ"
- `BTN_MY` = "📋 Мой заказ"
- `BTN_DONE` = "✅ Готово"

---

### 📦 **data/dishes.py** - Меню блюд

**DISHES_BY_LEVEL** - Словарь блюд по уровням:
- **Уровень 0**: 8 блюд (110-125 крестиков) - базовые напитки и закуски
- **Уровень 1**: 7 блюд (210-300 крестиков) - основные блюда
- **Уровень 2**: 3 блюда (350-400 крестиков) - более сложные блюда
- **Уровень 3**: 3 блюда (450-520 крестиков) - премиум блюда

**Специальные блюда:**
- `VIP_DISH`: ("🦪 Устрицы", 1000)
- `STUDENT_DISH`: ("🥡 Лапша быстрого приготовления", 100)
- `CAKE_DISH`: ("🎂 Свадебный торт", 1500)

---

### 📈 **data/levels.py** - Система уровней

**LEVELS** - Словарь названий уровней:
- 0: "🍳 Помощник повара"
- 1: "👨‍🍳 Повар"
- 2: "🧑‍🍳 Су-шеф"
- 3: "👩‍🍳 Шеф-повар"

**Константы:**
- `ORDERS_PER_LEVEL = 10` - Количество заказов для повышения уровня (10, 20, 30, 40)
- `MAX_LEVEL = 3` - Максимальный уровень

**Логика повышения:**
- Уровень повышается когда `total_orders % 10 == 0` и `level < 3`
- Помощник повара (1-10) → Повар (11-20) → Су-шеф (21-30) → Шеф-повар (31-40)

---

### 💬 **data/texts.py** - Текстовые шаблоны

Все сообщения бота хранятся здесь. Используется `.format()` для подстановки значений.

**Основные шаблоны:**
- `HELLO` - Приветствие
- `ALREADY_HAS_ORDER` - Уже есть активный заказ
- `NO_ACTIVE_ORDER` - Нет активного заказа
- `SHOW_ORDER_HEADER` - Заголовок при показе заказа
- `ORDER_TOTAL` - Информация о количестве крестиков
- `STUDENT_APPEAR`, `CRITIC_APPEAR`, `DIRTY_PLATE_APPEAR`, `SECOND_CHEF_APPEAR` - Сообщения о спецзаказах
- `DONE_ORDER`, `DONE_WITH_LEVEL_UP` - Сообщения о завершении заказа
- `TOP_SENT_DM`, `TOP_DM_FAIL` - Сообщения для статистики

---

### ⚙️ **config.py** - Конфигурация

Загружает переменные окружения из `.env`:
- `BOT_TOKEN` - Токен Telegram бота
- `CHAT_ID` - ID игрового чата
- `ADMIN_ID` - ID администраторов через запятую (например: "123,456")

---

## 🎯 Игровая механика

### Система уровней:
1. Начальный уровень: 0 (Помощник повара) - заказы 1-10
2. Повышение: каждые 10 завершённых заказов (после 10, 20, 30)
3. Максимальный уровень: 3 (Шеф-повар) - заказы 31-40

### Типы заказов:

#### 🎲 Специальные заказы (события):
- **Студент** (12%, 3-40 заказ, 1 раз): Лапша 100 крестиков
- **Критик** (10%, 20-40 заказ, 1 раз): Устрицы 1000 крестиков
- **Грязная тарелка** (15%, 3-40 заказ, 1 раз): Удваивает предыдущий заказ
- **Второй повар** (12%, 3-40 заказ, 1 раз): Следующий заказ половинный (не идет в счетчик)

#### 🍽️ Обычные заказы:
- 1 блюдо с текущего уровня
- 2 блюда из всех открытых уровней (случайно, без повторов)
- Всего 3 блюда в заказе

### Статистика:
- `total_orders` - Всего завершённых заказов
- `level` - Текущий уровень игрока
- Флаги специальных событий (студент, критик, грязная тарелка, второй повар)

---

## 🔍 Проверка логики

### ✅ Найденные проблемы и исправления:

1. **✅ ИСПРАВЛЕНО**: Переносы строк в `order.py`
   - Было: `"\\n"` (литеральная строка)
   - Стало: `"\n"` (символ новой строки)
   - Исправлено в строках 96, 97, 112, 114

### ⚠️ Потенциальные проблемы:

1. **Проверка чата**: Все игровые команды проверяют `CHAT_ID`, но нет проверки в `/start`
   - ✅ Это нормально, так как `/start` должна работать везде

2. **Обработка ошибок**: В `database.py` нет явной обработки ошибок при работе с БД
   - 💡 Рекомендуется добавить try-except блоки

3. **Валидация данных**: Нет проверки на пустые значения в конфиге
   - ⚠️ Может привести к ошибкам если переменные окружения не заданы

4. **Генерация заказа**: В `generate_regular_order` может быть бесконечный цикл если блюд недостаточно
   - 💡 Уже есть защита через `break`, но можно улучшить

---

## 📝 Рекомендации по улучшению:

1. ✅ Добавить логирование ошибок
2. ✅ Валидация входных данных
3. ✅ Обработка исключений в критических местах
4. ✅ Добавить проверку существования файла БД
5. ✅ Добавить бэкап базы данных

---

## 🚀 Запуск бота

1. Создать `.env` файл:
   ```
   BOT_TOKEN=ваш_токен
   CHAT_ID=id_чата
   ADMIN_ID=id_админа
   ```

2. Установить зависимости:
   ```bash
   pip install aiogram python-dotenv aiosqlite
   ```

3. Запустить:
   ```bash
   python main.py
   ```

---

**Версия документации:** 1.0  
**Дата создания:** 2024





