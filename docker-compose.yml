services:
  bot:
    build: .
    container_name: stitch-cafe-bot
    env_file: .env
    restart: unless-stopped
    volumes:
      - bot_data:/app/data
      - bot_logs:/app/logs

volumes:
  bot_data:
  bot_logs:
